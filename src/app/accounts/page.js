// ✅ Import from next/navigation (NOT next/router)


export default function Accounts() {
   // simulate unauthenticated

  // ✅ This must be synchronous at the top-level (not inside hooks or useEffect)
  

  return (
    <div>
      <h1>Account Page</h1>
    </div>
  );
}
